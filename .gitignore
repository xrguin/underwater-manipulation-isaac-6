# Python
__pycache__/
*.pyc
*.pyo

# Experiment outputs (npz logs, plots, Monte Carlo CSVs, videos) — kept local,
# except the frozen 20 Hz free-drift pilot datasets (train/val/test, seeds
# 101/202/303) shared for the Gaussian-vs-34D dictionary comparison.
EDMDc/data/*
!EDMDc/data/gaussian/
EDMDc/recording/

# Isaac assets are versioned. Large CAD/mesh USDs use Git LFS; see
# .gitattributes and assets/README.md for checkout instructions.

# Local agent/editor state
.claude/settings.local.json
.agents/
.codex/
nohup.out

# Literature PDFs — keep local (copyright)
*.pdf
